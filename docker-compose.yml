version: '3'

services:
  bootnode:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      - NODE_TYPE=bootnode
    volumes:
      - bootnode-keys:/keys
    networks:
      mev-net:
        ipv4_address: 172.28.0.2

  provider:
    build:
      context: .
      dockerfile: Dockerfile
    depends_on:
      - bootnode
    environment:
      - NODE_TYPE=provider
    volumes:
      - provider-keys:/keys
    networks:
      - mev-net

  user:
    build:
      context: .
      dockerfile: Dockerfile
    depends_on:
      - bootnode
    environment:
      - NODE_TYPE=user
    volumes:
      - user-keys:/keys
    networks:
      - mev-net
    ports:
      - "13523:13523"
      - "13524:13524"

volumes:
  bootnode-keys:
  provider-keys:
  user-keys:

networks:
  mev-net:
    ipam:
      driver: default
      config:
        - subnet: 172.28.0.0/16
